// eslint-disable-next-line consistent-return
// export default function isLoggedIn({ store, redirect }) {
//   if (!store.state.auth.access_token && !process.server) {
//     return redirect('/');
//   }
// }
